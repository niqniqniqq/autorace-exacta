# RUNBOOK: autorace-exacta (Local E2E → Prediction)

このドキュメントは、ローカル環境で **収集 → 学習 → 2連単予測 → API確認** までを最短で完走するための運用手順です。  
「壊れやすい箇所（収集/パース）」を機械的に検証するための **合格条件** と **切り分け手順**も含みます。

---

## 0. 前提

- Docker / Docker Compose が使えること
- `.env` のDB接続などが設定済み（またはデフォルトで動く）
- `docker compose up -d` で `db / api / worker` が立ち上がること

---

## 1. もっとも重要な設計方針（運用上の約束）

- 公開情報のみ（ログイン/回避/高負荷は禁止）
- 開催が無い日は **スキップ**して無駄アクセスしない（cron */5 でも安全にする）
- 取得データは raw スナップショット（内部用途）を保存し、DBには主に数値データを格納
- コマンドは **何度実行しても壊れない（idempotent / upsert）** ことを前提

---

## 2. ローカル起動

```bash
docker compose up -d --build
